---
"liquid-glass": minor
---

Add React glass primitives (`GlassNode`, `GlassSurface`, `LiquidGlass`), the shared
glass tint utilities (`GLASS_TINTS`, `createGlassTint`, `resolveGlassTint`,
`withTintBackgroundAlpha`), and the bundled `liquid-glass/styles.css` entry, along
with fixes to tint resolution and glass node styling from the current round of work.
